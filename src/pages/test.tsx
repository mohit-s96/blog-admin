import React, { ReactElement } from "react";
import AuthLoader from "../components/loaders/authLoader";

function Test(): ReactElement {
  return <AuthLoader theme="light" />;
}

export default Test;
